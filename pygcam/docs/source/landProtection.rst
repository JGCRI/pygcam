The ``pygcam.landProtection`` module
=====================================

This module provides all the functionality that is exposed through the
:doc:`gcamtool` sub-command `protectLand`. It is provided in library form
so that it can be used from within other programs without having to execute
the script.

API
---

.. automodule:: pygcam.landProtection
   :members:
