The ``pygcam.landProtection`` module
=====================================

This module implements the :doc:`gcamtool` sub-command `protectLand`. It is
provided in library form so that it can be used from within setup programs
(or other programs) without having to execute the gcamtool script.

API
---

.. automodule:: pygcam.landProtection
   :members:
